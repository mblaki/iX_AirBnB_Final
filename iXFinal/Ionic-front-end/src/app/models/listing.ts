// class which specifies what our listing model should look like
export class Listing {
        id: number;
        title: string;
        location: string;
        description: string;
        capacity: number;
        price: number;
        hostId: number;  
}
